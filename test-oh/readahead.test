#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

ohosonly testcmd "help" "--help >/dev/null 2>&1 && echo ok" "ok\n" "" ""

# no args should do nothing (just exits 0)
ohosonly testcmd "no args" "$C 2>&1; echo \$?" "0\n" "" ""
