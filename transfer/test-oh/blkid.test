#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

# blkid requires filesystem image files which are not available on device.
ohosonly testing "--help" "$C --help 2>&1 | head -1" "blkid: usage\n" "" ""
