#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

# exit codes & no output
ohosonly testing "exit 0" "$C && echo yes" "yes\n" "" ""
ohosonly testing "exit 0 with args" "$C foo && echo yes" "yes\n" "" ""
