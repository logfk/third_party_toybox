#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

ohosonly testing "header" "$C | grep -q . && echo yes" "yes\n" "" ""
ohosonly testing "runs" "$C >/dev/null && echo yes" "yes\n" "" ""
