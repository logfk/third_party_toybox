#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

ohosonly testcmd "help" "--help 2>&1 | grep -qi 'setsid' && echo ok" "ok\n" "" ""

# run a command in a new session
ohosonly testcmd "run cmd" "$C /bin/true && echo ok" "ok\n" "" ""
