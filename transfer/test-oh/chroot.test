#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

# CFG_TOYBOX_HELP_DASHDASH=0: --help not intercepted, becomes NEWROOT arg → exit 125
ohosonly testcmd "help" "--help >/dev/null 2>&1; echo \$?" "125\n" "" ""

# no args should fail
ohosonly testcmd "no args" "$C 2>/dev/null; echo \$?" "125\n" "" ""

# non-existent root should fail
ohosonly testcmd "bad root" "$C /nonexistent true 2>/dev/null; echo \$?" "125\n" "" ""
