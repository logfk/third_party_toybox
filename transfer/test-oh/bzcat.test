#!/bin/bash

[ -f testing.sh ] && . testing.sh

#testing "name" "command" "result" "infile" "stdin"

# bzcat tests require compressed test data files ($FILES) not available on device.
ohosonly testing "--help" "$C --help 2>&1 | head -1" "bzcat: usage\n" "" ""
